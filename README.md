# Reversal

Command+Z for a living room, executed by a humanoid.

This prototype tests one assumption: when something in the home changes, people will tap **Undo** and wait for a robot instead of fixing it themselves.

The room is simulated. Undo/redo is real (stack-based, including ⌘Z / ⌘⇧Z).

```bash
npm run dev
```
